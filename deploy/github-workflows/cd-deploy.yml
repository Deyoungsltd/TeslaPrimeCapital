name: CD Coolify Deployment

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Zero-Downtime Deployment
        run: |
          echo "Triggering Coolify webhook for rolling container deployment..."
          # curl -X POST ${{ secrets.COOLIFY_WEBHOOK_URL }} -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}"
